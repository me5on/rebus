const ASSERT = Object.freeze({

    // at the beginning of the string
    bgn: '^',

    // at the end of the string
    end: '$',

    // positive word boundary (boundary exists)
    pwb: '\\b',

    // negative word boundary (boundary doesn't exist)
    nwb: '\\B',
    
});


export default ASSERT;
